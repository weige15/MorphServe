# Historical-result classification

These files are an immutable copy of the successful result first committed in `e4cc016f3cb6d25ddf36e59e5a05fc21cae5f73c` (`research(results): verify transactional real GPU executor`). They predate the asynchronous copier and later atomic post-shrink recovery/poisoning repairs.

They remain evidence for the historical modified-condition implementation only. They are not current-revision validation. The current `results/` directory is reserved for the queued rerun and may be overwritten by `run_real_executor_pilot.sh`.
