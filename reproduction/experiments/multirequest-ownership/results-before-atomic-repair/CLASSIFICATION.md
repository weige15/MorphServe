# Historical-result classification

These files are an immutable copy of the successful result first committed in `967671bc64e81b3cc6303ce15eaa5f5cec222bbb` (`research(results): verify real multi-request ownership recovery`). They predate the asynchronous copier and later atomic post-shrink recovery/poisoning repairs.

They remain evidence for the historical modified-condition implementation only. They are not current-revision validation. The current `results/` directory is reserved for the queued rerun and may be overwritten by `run_multirequest_ownership.sh`.
